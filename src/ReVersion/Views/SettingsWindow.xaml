<Window x:Class="ReVersion.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReVersion.Views"
        xmlns:m="clr-namespace:ReVersion.Models"
        xmlns:vw="clr-namespace:ReVersion.Views.Settings"
        mc:Ignorable="d"
        Title="Settings" Height="300" Width="300" Loaded="SettingsWindow_OnLoaded">
    <Window.Resources>
        <DataTemplate DataType="{x:Type m:SvnServer}">
            <vw:SvnServer></vw:SvnServer>
        </DataTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Label Content="Svn Servers"></Label>
        
        <StackPanel Orientation="Vertical" Grid.Column="1">

            <ItemsControl ItemsSource="{Binding Servers}">
            </ItemsControl>

        </StackPanel>

    </Grid>
</Window>
